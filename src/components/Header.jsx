import React from 'react';

const Header = () => {
  return (
    <header style={{ background: '#FFCCCC', padding: '1rem', textAlign: 'center' }}>
      Header
    </header>
  );
};

export default Header;